name: CI

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v3

    - name: Build copy to Hosted Location
      shell: pwsh
      run: | 
        Copy-Item -Path ".\*" -Destination "C:\inetpub\dsdevops" -Recurse
